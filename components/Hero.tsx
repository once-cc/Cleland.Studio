import React from 'react';
import { Hero21st } from '@/components/hero/hero-21st';

export const Hero: React.FC = () => {
    return (
        <section id="hero" className="relative w-full bg-studio-bg">
            <Hero21st />
        </section>
    );
};
